services:
  dsm:
    container_name: dsm
    image: vdsm/virtual-dsm:7.50
    environment:
      DISK_SIZE: "16G"
    # devices:
    #   - /dev/kvm
    cap_add:
      - NET_ADMIN
    ports:
      - 5000:5000
    volumes:
      - ./dsm:/storage
    stop_grace_period: 2m
    network_mode: bridge
